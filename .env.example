# Required only for `npm run test:integration`, which exercises the plugin
# against a live network. Unit tests need none of this.
HEDERA_NETWORK=testnet
HEDERA_ACCOUNT_ID=0.0.xxxxxxx
# Hex-encoded ECDSA secp256k1 key (0x + 64 hex chars)
HEDERA_PRIVATE_KEY=
